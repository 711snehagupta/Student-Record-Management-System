# Student-Record-Management-System
Student Record Management System is a Python CLI project that manages student records using add, view, update, and delete operations. It uses Python functions, dictionaries, and JSON file handling for data storage and management.
